name: Build Firmware

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-keyboards:
    name: Build ${{ matrix.keyboard }} (${{ matrix.mcu }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - keyboard: corne
            mcu: milandr
            args: "-c"
          - keyboard: corne
            mcu: rp2040
            args: "-c --uf2"
          - keyboard: jianovka
            mcu: milandr
            args: "-c"
          - keyboard: kabarga
            mcu: nrf52840
            args: "-c --uf2"
          - keyboard: magneteno
            mcu: rp2350
            args: "-c --uf2"
          - keyboard: nizkoteno
            mcu: rp2040
            args: "-c --uf2"
          - keyboard: omsk
            mcu: rp2350
            args: "-c --uf2"
          - keyboard: pncateho
            mcu: nrf52840
            args: "-c --uf2"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Toolchain and Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build gcc-arm-none-eabi libnewlib-arm-none-eabi

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Build Firmware (${{ matrix.keyboard }} / ${{ matrix.mcu }})
        run: |
          chmod +x build_all.sh
          ./build_all.sh -b ${{ matrix.keyboard }} --mcu ${{ matrix.mcu }} ${{ matrix.args }}

      - name: Upload Firmware Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.keyboard }}-${{ matrix.mcu }}
          path: |
            build/dmk_*
          if-no-files-found: error
